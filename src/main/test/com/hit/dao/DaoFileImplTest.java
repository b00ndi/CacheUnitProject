package com.hit.dao;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.Assert;
import org.junit.Ignore;
import org.junit.jupiter.api.Test;

import com.hit.dm.DataModel;

@Ignore
class DaoFileImplTest {

	@Test
	void test()
	{
//		String filePath = "dataSourceTest.txt";
//		DaoFileImpl dao = new DaoFileImpl(filePath, 3);
//
//		dao.save(new DataModel<Integer>((long)1, 1));
//		dao.save(new DataModel<Integer>((long)2, 2));
//		dao.save(new DataModel<Integer>((long)3, 3));
//		
//		DataModel<Integer> dm = dao.find((long)1);
//		Assert.assertEquals((long)1, (long)dm.getContent());
//		
//		dm = dao.find((long)2);
//		Assert.assertEquals((long)2, (long)dm.getContent());
//		
//		dm = dao.find((long)3);
//		Assert.assertEquals((long)3, (long)dm.getContent());
	}

}
